#include <stdio.h>
#include <math.h>

int main() {
    int i, temp, remainder, digits, result;

    printf("Armstrong numbers between 1 and 1000 are:\n");

    for (i = 1; i <= 1000; i++) {
        temp = i;
        digits = 0;
        result = 0;







































































































































































































































































































































































































































































        while (temp != 0) {
            temp /= 10;
            digits++;
        }

        temp = i;

    
        while (temp != 0) {
            remainder = temp % 10;
            result += round(pow(remainder, digits));
            temp /= 10;
        }

    
        if (result == i) {
            printf("%d ", i);
        }
    }
    printf("\n");

    return 0;
}